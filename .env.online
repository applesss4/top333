# 线上 Supabase 配置
# 注意：请替换为线上 Supabase 项目的实际配置

# 线上 Supabase 项目 URL
ONLINE_SUPABASE_URL=https://your-online-project.supabase.co

# 线上 Supabase 服务角色密钥（私有密钥，仅用于服务端）
ONLINE_SUPABASE_SERVICE_ROLE_KEY=your-online-service-role-key

# 线上 Supabase 匿名密钥（公开密钥）
ONLINE_SUPABASE_ANON_KEY=your-online-anon-key

# 线上 JWT 密钥（用于验证 Supabase JWT）
ONLINE_SUPABASE_JWT_SECRET=your-online-jwt-secret

# 重要提示：
# 1. 请确保线上 Supabase 项目已创建相同的表结构
# 2. 可以使用 setup-supabase-database.sql 脚本在线上项目中创建表结构